{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "运行 Silk Math Preview",
      "type": "extensionHost",
      "request": "launch",
      "args": ["--extensionDevelopmentPath=${workspaceFolder}"],
      "outFiles": ["${workspaceFolder}/dist/**/*.js"],
      "preLaunchTask": "Silk Math: build"
    }
  ]
}
