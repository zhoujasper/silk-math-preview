\documentclass{article}
% 案例 A：同一文件先写宏，再写后面的公式。
% 把光标分别放进下面每条 $...$。

$x$

\newcommand{\keep}{K}
$\keep$

\newcommand{\later}{\alpha}
$\later$

\newcommand{\ren}{r}
\renewcommand{\ren}{R}
$\ren$

\newcommand{\pair}[2]{(#1,#2)}
$\pair{a}{b}$

% A5：在下面加 \newcommand{\fresh}{\beta}，再写 $\fresh$
